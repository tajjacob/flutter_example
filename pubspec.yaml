name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

#  aad_oauth: ^0.4.4 # Uncomment this line if there is new version of aad_oauth
  aad_oauth:
    git:
      url: https://github.com/tajjacob/aad_oauth.git
      ref: upstream/2023-33-upgrade-plugins+add-param
  health: ^8.0.0
  flutter_local_notifications: ^15.1.1
  health_kit: ^0.0.6
  permission_handler: ^10.4.3
  provider: ^6.0.5
  d_chart: ^2.6.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.2

flutter:

  uses-material-design: true


  assets:
    - assets/
    - assets/json/

